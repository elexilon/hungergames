import  create, { CREATE_GAME } from './create'
import fetchGames, { fetchOneGame, FETCHED_GAMES, FETCHED_ONE_GAME } from './fetch'
import update, { UPDATE_GAME, updateWeight } from './update'
import fetchPlayers, { FETCHED_PLAYERS } from './player/fetch'

export {
  create,
  CREATE_GAME,
  fetchGames,
  fetchOneGame,
  FETCHED_GAMES,
  FETCHED_ONE_GAME,
  update,
  UPDATE_GAME,
  updateWeight,
  fetchPlayers,
  FETCHED_PLAYERS
}
